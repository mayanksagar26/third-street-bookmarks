# Personal data — never commit
bookmarks.json
settings.json

# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Build output
client/dist/

# Env & secrets
.env
.env.local

# OS
.DS_Store
.gstack/

# If TSB_DATA_DIR points at the repo: the API credential, per-source
# collections, uploaded exports and the state DB must never be committable.
auth-token
sources/
imports/
state.db
